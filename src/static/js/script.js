// General JavaScript file (can be expanded later)

// Example: Delete confirmation logic (already included in dashboard.html block,
// but could be moved here for centralization if preferred)

// document.addEventListener('DOMContentLoaded', function() {
//     const deleteForms = document.querySelectorAll('.delete-form');
//     deleteForms.forEach(form => {
//         form.addEventListener('submit', function(event) {
//             if (!confirm('Are you sure you want to delete this record? This action cannot be undone.')) {
//                 event.preventDefault(); // Stop form submission
//             }
//         });
//     });
// });

console.log("Script loaded."); // For checking if the file is linked correctly